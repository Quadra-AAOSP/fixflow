# Development: the browser uses same-origin /api (including the session cookie).
API_PROXY_TARGET=http://127.0.0.1:8080
# Production: keep empty when your server reverse-proxies /api to Spring Boot.
# For a separate API origin, set its URL and configure backend CORS/cookies.
VITE_API_URL=
